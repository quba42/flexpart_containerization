
* install the following dev packages
  libgrib-api-dev
  libjasper-dev
  gfortran (actually not a dev pkg ;) )

* unpack flexpart-code-custom-to-QP.tar.gz
  compile FLEXPART by just typing "make clean ; make" in FLEXPART directory -> will generate binary "flextra.gfs" if everything is OK

* unpack example directory from flexpart-

* go to example directory

* execute ./01-makepathnames.sh

* mount weather data repository:
  sshfs -p 2222 weatherdata@141.40.254.10:/weatherdata/GFS GFS

* cat GFS/AVAILABLE gives you an overview of the wind-field files from GFS (global forecast system) available in AlpEnDAC (for all the dates)

* copy flexpart binary:
  copy <compiledirectory>/flexpart.gfs .

* ./04-flexpartrun.sh 



